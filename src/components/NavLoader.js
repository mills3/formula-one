import React from 'react';
import Logo from './Logo';

const NavLoader = () => {
  return (  
    <div className="nav">
      <Logo width={'300px'} animate={true} />
    </div>
  );
}
 
export default NavLoader;